$( "#artists" ).on( "click", function( event ) {
    artists.load()
})

$( "#albums" ).on( "click", function( event ) {
    albums.load()
})

$("#tracks").on("click", function(event) {
    tracks.load()
})

